# Architecture Design Agent

Provides candidate generation, tradeoff analysis, and pattern composition utilities used by the `ArchitectureDesignAgent`.

This package is fully self-contained, deterministic, and uses only the `AgentContext` and knowledge interfaces provided at runtime.
# Architecture Design Agent

Repository skeleton.

Authority:
REPOSITORY_MASTER_STRUCTURE.md

No implementation yet.
