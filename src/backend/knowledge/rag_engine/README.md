RAG Engine
==========

Composes semantic search, metadata filtering, ranking and context building. This
module is intentionally decoupled from any LLM or external services and is used
for unit-level validation of discovery behaviors.
